syntax = "proto3";

package proto;

message Person {
    string name = 1;
    int32 age = 2;
}
message Card {
    Person person = 1;
    repeated string hobbies = 2;
    map<string, int32> keyword = 3;
}
